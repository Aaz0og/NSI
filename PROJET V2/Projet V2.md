```pseudo-py
Pour toute la liste la plus petite: 
	Pour toute la liste qui reste: 
		Pour tout les numéros dans x: 
			Variable = numéro X actuel - numéro avant
			Pour tout les numéros dans y:
				Si Variable = y:
					Solution += 1
return sol
```